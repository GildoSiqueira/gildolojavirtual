@using Gildo.lojaVirtual.Web.HtmlHelpers

@model Gildo.lojaVirtual.Web.Models.ProdutosViewModel

@foreach (var p in Model.Protudos)
{       
    <div>
        <h3>@p.Nome</h3>
        @p.Descricao
        <h3>@p.Preco.ToString("c")</h3>       
    </div>
}

